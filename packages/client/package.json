{
  "name": "shift-manager-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx postgraphile -c postgres://postgres:postgres@localhost:5432/shift-manager -s public --watch --enhance-graphiql --cors --dynamic-json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "rxdb": "^14.17.1"
  }
}
